name: Node.js CI

on:
  push:
    branches: ["main", "prod"]
  pull_request:
    branches: ["main", "prod"]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      - run: npm ci
      - run: npm run build --if-present

      - name: Deploy with gh-pages for main
        if: github.ref == 'refs/heads/main'
        run: |
          git remote set-url origin https://git:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          npx gh-pages -d build -u "github-actions-bot <support+actions@github.com>" -r https://github.com/${{ github.repository }}.git -b gh-pages -m "Deploy main" --dest main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy with gh-pages for prod
        if: github.ref == 'refs/heads/prod'
        run: |
          git remote set-url origin https://git:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          npx gh-pages -d build -u "github-actions-bot <support+actions@github.com>" -r https://github.com/${{ github.repository }}.git -b gh-pages -m "Deploy prod" --dest prod
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
